<Window x:Class="ClothingStore.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="240" Width="360" ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <ResourceDictionary Source="/Resources/Styles.xaml"/>
    </Window.Resources>

    <Grid Margin="16" RowDefinitions="Auto,Auto,Auto,Auto" >
        <TextBlock Text="Email" Margin="0,0,0,4"/>
        <TextBox Name="txtEmail" Grid.Row="1" Height="28" />
        <TextBlock Text="Password" Grid.Row="2" Margin="0,8,0,4"/>
        <PasswordBox Name="txtPassword" Grid.Row="3" Height="28"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0" >
            <Button Name="btnLogin" Click="BtnLogin_Click" Content="Login" Width="90" Margin="0,0,8,0" Style="{StaticResource PrimaryButton}"/>
            <Button Name="btnRegister" Click="BtnRegister_Click" Content="Register" Width="90" Style="{StaticResource SecondaryButton}"/>
        </StackPanel>
    </Grid>
</Window>
